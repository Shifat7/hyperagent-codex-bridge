# v0.5.0 controlled live demo receipt

Date: July 21, 2026

Status: **PASS**

## Controlled route

```text
Codex 0.144.6
-> local v0.5.0 Responses bridge on 127.0.0.1
-> documented Hyperagent OAuth MCP
-> pinned hyperagent/sol relay
-> typed function_call
-> Codex local shell execution
-> tool result returned to Hyperagent
-> typed final response
```

The proof used a temporary, explicitly approved 12-request ceiling because the original 10-request ceiling had already been reached. It consumed exactly two additional requests. The original ceiling was restored immediately afterward; the bridge was stopped.

## Sanitized receipt

| Request | Model | Hyperagent thread | Output | Prompt chars | Forwarded tools | Total latency |
| --- | --- | --- | --- | ---: | ---: | ---: |
| `hacb_f821a9d4c35b479b836cec670aee77c4` | `hyperagent/sol` | `cmrv1nfrk01pl08adsxsnffv1` | `function_call` | 31,572 | 32 | 13,769 ms |
| `hacb_49d06bc695be403793796048bac80ec3` | `hyperagent/sol` | `cmrv1np1n01wz07adtzrvrj8p` | `final` | 32,012 | 32 | 11,840 ms |

Summary:

```text
requests: 2
completed: 2
failed: 0
incomplete: 0
p50 request latency: 11,840 ms
p95 request latency: 13,769 ms
wall time: 27,146 ms
```

## Privacy boundary

This artifact contains timestamps, bridge request IDs, model slug, Hyperagent thread IDs, completion types, character/tool counts, and latency only. It contains no prompt, answer, OAuth token, refresh token, local bearer, Codex authentication, or private state-file content.

## Cost boundary

The supported Hyperagent MCP thread result does not expose machine-readable per-request usage or cost. This receipt proves the route, two-request tool loop, and latency. A precise credit delta still requires an operator-observed Hyperagent account balance before and after the run.
