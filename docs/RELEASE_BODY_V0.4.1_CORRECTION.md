# Required correction to the already-public v0.4.1 GitHub release body

Do not publish automatically. Edit only after owner approval.

Replace:

> Add a persistent 20-request UTC daily ceiling and `hacb budget` command.

With:

> Add a persistent six-request UTC daily ceiling and `hacb budget` command.

The v0.4.1 tag, published ZIP, README, changelog, proof document, and tests all use six. The current GitHub release body is the inconsistent artifact.
